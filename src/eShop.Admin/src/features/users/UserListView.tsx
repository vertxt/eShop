import { Typography } from "@mui/material";

export default function UserListView() {
    return (
        <>
            <Typography>This is user list view</Typography>
            <Typography>Currently in development... Stay tuned.</Typography>
        </>
    )
}